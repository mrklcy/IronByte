Realtime features can be added here with Socket.IO or native WebSocket adapters.

Recommended channels:
- `notifications:user:{userId}`
- `labs:attempt:{attemptId}`
- `leaderboard:global`
- `teams:{teamId}`
